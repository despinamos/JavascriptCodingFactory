// named exports
const PI = 3.14
const EPSILON = 0.000005
const API_URL = "https://codingfactory.aueb.gr"

//default export - only one can be default
const SEC_PER_MIN = 60
export default SEC_PER_MIN

// only for named exports can we can this form
export { PI, EPSILON, API_URL }